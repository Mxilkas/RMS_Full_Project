# RentalPro RMS React Frontend

## Start

```bash
npm install
npm run dev
```

API base URL:

```env
VITE_API_URL=http://localhost:5107/api
```

The frontend uses Axios, React Router and reusable CRUD hooks/components. Routes are defined in `src/main.jsx`.
